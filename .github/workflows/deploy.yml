name: Deploy

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: '🚀 배포할 환경을 선택하세요 (Default: dev)'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod
      log_level:
        description: '📝 애플리케이션 로그 레벨'
        required: true
        default: 'INFO'
        type: choice
        options:
          - DEBUG
          - INFO
          - WARN
          - ERROR
  push:
    branches:
      - main

env:
  DOCKER_NETWORK: app-network
  CONTAINER_NAME: dev-ticket-container

jobs:
  build-app:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java-version: [ '21' ]

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java-version }}

      - name: Grant execute permission for Gradle
        working-directory: ./
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew clean build -x test

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v6
        with:
          name: built-jar
          path: build/libs/*.jar

  docker-push:
    needs: build-app
    runs-on: ubuntu-latest

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Download Build Artifact
        uses: actions/download-artifact@v7
        with:
          name: built-jar
          path: build/libs/

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Get Short SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ticket-api:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ticket-api:${{ steps.vars.outputs.sha_short }}

  deploy-ec2:
    needs: docker-push
    runs-on: ubuntu-latest
    steps:
      - name: Set Environment Variables
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "TARGET_ENV=${{ inputs.target_env }}" >> $GITHUB_ENV
            echo "LOG_LEVEL=${{ inputs.log_level }}" >> $GITHUB_ENV
          
          elif [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "TARGET_ENV=prod" >> $GITHUB_ENV
            echo "LOG_LEVEL=INFO" >> $GITHUB_ENV
          
          else
            echo "TARGET_ENV=dev" >> $GITHUB_ENV
            echo "LOG_LEVEL=DEBUG" >> $GITHUB_ENV
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Get GitHub Actions Runner IP
        id: ip
        run: |
          echo "ipv4=$(curl -s https://checkip.amazonaws.com)" >> $GITHUB_OUTPUT

      - name: Add GitHub IP to Security Group
        run: |
          aws ec2 authorize-security-group-ingress \
            --group-id ${{ secrets.EC2_SECURITY_GROUP_ID }} \
            --protocol tcp \
            --port 22 \
            --cidr ${{ steps.ip.outputs.ipv4 }}/32

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/ticket-api:latest
            sudo docker stop ${{ env.CONTAINER_NAME }} || true
            sudo docker rm ${{ env.CONTAINER_NAME }} || true
            sudo docker run -d \
              --name ${{ env.CONTAINER_NAME }} \
              --network ${{ env.DOCKER_NETWORK }} \
              -e SPRING_PROFILES_ACTIVE=${{ env.TARGET_ENV }} \
              -e DB_HOST=${{ secrets.DB_HOST }} \
              -e DB_USER=${{ secrets.DB_USER }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
              -e REDIS_HOST=${{ secrets.REDIS_HOST }} \
              -e REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }} \
              -e SMTP_HOST=${{ secrets.SMTP_HOST }} \
              -e SMTP_PORT=${{ secrets.SMTP_PORT }} \
              -e SMTP_USERNAME=${{ secrets.SMTP_USERNAME }} \
              -e SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }} \
              -e SMTP_FROM=${{ secrets.SMTP_FROM }} \
              -e OAUTH2_GOOGLE_CLIENT_ID=${{ secrets.OAUTH2_GOOGLE_CLIENT_ID }} \
              -e OAUTH2_GOOGLE_CLIENT_SECRET=${{ secrets.OAUTH2_GOOGLE_CLIENT_SECRET }} \
              -e OAUTH2_GOOGLE_REDIRECT_URI=${{ secrets.OAUTH2_GOOGLE_REDIRECT_URI }} \
              -e OAUTH2_NAVER_CLIENT_ID=${{ secrets.OAUTH2_NAVER_CLIENT_ID }} \
              -e OAUTH2_NAVER_CLIENT_SECRET=${{ secrets.OAUTH2_NAVER_CLIENT_SECRET }} \
              -e OAUTH2_NAVER_REDIRECT_URI=${{ secrets.OAUTH2_NAVER_REDIRECT_URI }} \
              -e OAUTH2_KAKAO_CLIENT_ID=${{ secrets.OAUTH2_KAKAO_CLIENT_ID }} \
              -e OAUTH2_KAKAO_CLIENT_SECRET=${{ secrets.OAUTH2_KAKAO_CLIENT_SECRET }} \
              -e OAUTH2_KAKAO_REDIRECT_URI=${{ secrets.OAUTH2_KAKAO_REDIRECT_URI }} \
              -e JWT_ISSUER=${{ secrets.JWT_ISSUER }} \
              -e JWT_ACCESS_TOKEN_SECRET=${{ secrets.JWT_ACCESS_TOKEN_SECRET }} \
              -e JWT_REFRESH_TOKEN_SECRET=${{ secrets.JWT_REFRESH_TOKEN_SECRET }} \
              -e EMAIL_VERIFICATION_BASE_URL=${{ secrets.EMAIL_VERIFICATION_BASE_URL }} \
              -e EMAIL_PASSWORD_RESET_BASE_URL=${{ secrets.EMAIL_PASSWORD_RESET_BASE_URL }} \
              -e PG_PAYMENT_PORTONE_STORE_ID=${{ secrets.PG_PAYMENT_PORTONE_STORE_ID }} \
              -e PG_PAYMENT_PORTONE_BASE_URL=${{ secrets.PG_PAYMENT_PORTONE_BASE_URL }} \
              -e PG_PAYMENT_PORTONE_SECRET_KEY=${{ secrets.PG_PAYMENT_PORTONE_SECRET_KEY }} \
              -e PG_PAYMENT_PORTONE_TIMEOUT_MILLIS=${{ secrets.PG_PAYMENT_PORTONE_TIMEOUT_MILLIS }} \
              -p 8080:8080 \
              --log-opt max-size=10m \
              --log-opt max-file=3 \
              --log-opt compress=true \
              ${{ secrets.DOCKERHUB_USERNAME }}/ticket-api:latest

      - name: Remove GitHub IP from Security Group
        if: always()
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id ${{ secrets.EC2_SECURITY_GROUP_ID }} \
            --protocol tcp \
            --port 22 \
            --cidr ${{ steps.ip.outputs.ipv4 }}/32